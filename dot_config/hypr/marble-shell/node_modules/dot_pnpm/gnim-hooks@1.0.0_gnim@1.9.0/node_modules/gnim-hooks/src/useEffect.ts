import { Accessor, createEffect } from "gnim"

/** @deprecated Use {@link createEffect} */
export function useEffect(fn: (track: <T>(signal: Accessor<T>) => T) => void) {
    return createEffect(() => {
        fn((v) => v())
    })
}
